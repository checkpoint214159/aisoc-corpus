#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel 2>/dev/null || true)"
if [[ -z "${ROOT_DIR}" ]]; then
  # Not a git repo; nothing to do.
  exit 0
fi

"${ROOT_DIR}/scripts/check.sh"
"${ROOT_DIR}/scripts/validate-content.sh"
